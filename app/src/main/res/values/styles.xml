<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MainTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/colorWhite</item>
        <item name="colorPrimary">@color/colorWhite</item>
        <item name="colorPrimaryDark">@color/colorWhite</item>
        <item name="colorAccent">@color/colorGreen</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="SignTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/colorWhite</item>
        <item name="colorPrimary">@color/colorWhite</item>
        <item name="colorPrimaryDark">@color/colorWhite</item>
        <item name="colorAccent">@color/colorGreen</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- edittext 스타일 지정-->
    <style name="EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">@color/colorWhite</item> <!--포커스 되어있지 않은 경우의 밑줄 부분-->
        <item name="colorControlActivated">@color/colorWhite</item> <!--손끝으로 터치되어 있는 부분. text selection handle라고 부른다고 한다-->
        <item name="android:textCursorDrawable">@color/colorWhite</item> <!--커서 색. drawable 지정되지만, 색을 설정하는것도 된다-->
        <item name="android:textColor">@color/colorWhite</item> <!--텍스트 색-->
        <item name="android:textColorHint">@color/colorWhite</item> <!--입력전의 Hint (포커스 Holder)-->
        <item name="android:textColorHighlight">@color/colorWhite</item> <!--텍스트 선택색-->
    </style>

    <style name="signEditText"  parent="Widget.AppCompat.EditText">x
        <item name="colorControlNormal">@color/colorGreen</item> <!--포커스 되어있지 않은 경우의 밑줄 부분-->
        <item name="colorControlActivated">@color/colorWhite</item> <!--손끝으로 터치되어 있는 부분. text selection handle라고 부른다고 한다-->
        <item name="android:textCursorDrawable">@color/colorGray</item> <!--커서 색. drawable 지정되지만, 색을 설정하는것도 된다-->
        <item name="android:textColor">@color/colorBlack</item> <!--텍스트 색-->
        <item name="android:textColorHint">@color/colorGray</item> <!--입력전의 Hint (포커스 Holder)-->
        <item name="android:textColorHighlight">@color/colorGray</item> <!--텍스트 선택색-->
    </style>

</resources>
